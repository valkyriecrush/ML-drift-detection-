# --- agentic_drift_stress (agent) ---
langgraph>=0.2
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

# --- drift_simulator (pipeline reelle : preprocessing + modele LightGBM) ---
pyyaml>=6.0
lightgbm>=4.0
xgboost>=2.0
joblib>=1.3
shap>=0.45

# --- alerting : fichier local (toujours) + serveur mock optionnel + vrai SMTP optionnel ---
requests>=2.31
fastapi>=0.110
uvicorn[standard]>=0.27
python-dotenv>=1.0

# --- diagnostic report -> PDF (pure Python, aucune lib systeme type wkhtmltopdf/Cairo) ---
markdown>=3.5
xhtml2pdf>=0.2.14
# smtplib (envoi SMTP reel, bridge/configure_real_channels.py) : stdlib, rien a installer
